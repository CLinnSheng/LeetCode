#include <bits/stdc++.h>
using namespace std;

/*
 * Each player can take all the stones in the first X remaining piles, where 1 <= X <= 2M.
 * Then set M = max(M, X). Initially, M = 1
 * Goal: maximum stones get
 */
class Solution
{
  public:
    int stoneGameII(vector<int> &piles)
    {
    }
};
